FROM adminer

ENV ADMINER_SERVER=mariadb
ENV ADMINER_DB_NAME="{{ CLOUD1_DB_NAME }}"
ENV ADMINER_DB_USER="{{ MARIADB_USER }}"
ENV ADMINER_DB_PASSWORD="{{ MARIADB_PASSWORD }}"
ENV ADMINER_DESIGN='nette'